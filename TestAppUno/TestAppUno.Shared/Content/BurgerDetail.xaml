<Page
    x:Class="TestAppUno.Shared.Content.BurgerDetail"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestAppUno.Shared.Content"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="5, 0, 0, 5">

    <StackPanel>
        <Image Width="100" Height="100" Source="{Binding SelectedBurger.ImagePath}" />
        <TextBlock Text="{Binding SelectedBurger.Name}" FontSize="25" />
        <TextBlock Text="{Binding SelectedBurger.ShortDescription}" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Stretch" />
        <TextBlock Text="{Binding SelectedBurger.Price}" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Stretch" />

        <TextBox Width="100" Text="{Binding Amount, Mode=TwoWay}"></TextBox>
        <!--<Entry WidthRequest="100" Text="{Binding Amount, Mode=TwoWay}"></Entry>-->
        <Button Content="Add to cart" Click="ButtonBase_OnClick"></Button>
        <Button Content="Read description" Command="{Binding ReadDescriptionCommand}"></Button>
    </StackPanel>
</Page>
